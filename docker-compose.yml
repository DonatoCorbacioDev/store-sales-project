services:
  forecasting-api:
    build:
      context: .
      dockerfile: api/Dockerfile
    container_name: forecasting-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped